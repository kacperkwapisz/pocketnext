/**
 * PocketBase Integration
 *
 * ⚠️ IMPORTANT: Do not mix client and server imports! ⚠️
 *
 * For client components:
 *   import { pb, isAuthenticated } from "@/lib/pocketbase/client";
 *
 * For server components:
 *   import { createClient } from "@/lib/pocketbase/server";
 *
 * This file provides only documentation and types.
 */

// Deliberate empty export to make this a module
export type {};
