/**
 * DB Package for PocketBase integration
 *
 * This package provides typed access to PocketBase collections.
 * It ONLY contains types - no runtime code.
 *
 * Each app creates its own PocketBase instances optimized for
 * client or server contexts.
 */

// Export only types
export * from "./types";
